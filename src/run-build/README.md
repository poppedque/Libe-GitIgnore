### Notice (only for bash)
**Permissions** Before you can run the script, you must grant it execution permissions. In your terminal, run:
`chmod +x run.sh`
You can then execute it by running:
`./run.sh`
